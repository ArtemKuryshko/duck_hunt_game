[pytest]
testpaths = tests
addopts = -ra
markers =
    systems: tests for system-layer classes
    io: tests that exercise filesystem persistence behavior
    unit: isolated unit tests with stubs or mocks
